#define STATUS_STATE_MASK   GENMASK(3, 0)   /* bits [3:0] */
#define STATUS_ERR_MASK     GENMASK(7, 4)   /* bits [7:4] */


#define BIT(n) (1UL << (n))

#define GENMASK(h, l) \
    (((~0UL) << (l)) & (~0UL >> (BITS_PER_LONG - 1 - (h))))


#define OPCODE(n) ( (( n & ~0x03FF) >> 24))

//Take a look to field bits